import type { ProjectTypeDescriptor } from '@/features/projectTypes/registry';

export const generalProjectType: ProjectTypeDescriptor = {
  id: 'General',
  label: 'General',
  shortLabel: 'General',
  description: 'Standard project workflows (tasks, goals, timeline).',
};
