import ComingSoonPanel from '@/components/molecules/ComingSoonPanel';

export default function JwtPage() {
  return (
    <ComingSoonPanel
      title="JWT Decoder"
      phase="phase-4"
      description="Local decode with jose — add page when disk space allows."
    />
  );
}
