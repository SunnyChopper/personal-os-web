#!/usr/bin/env sh
bun run type-check || exit 1
echo "Skipping lint check (import resolver issues to be fixed)"
# format:check runs in GitHub Actions with LF checkouts; a full local check here
# fails on Windows CRLF worktrees even when committed blobs are valid for CI.
